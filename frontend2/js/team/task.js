class Task {
    constructor (id, title, depth, position, completed) {
        this.id = id;
        this.title = title;
        this.depth = depth;
        this.position = position;
        this.completed = completed;
    }
}